packages:
  github.com/authorhealth/go-exercise:
    config:
      all: true
      dir: "{{ .InterfaceDir }}"
      filename: "mock_{{ .InterfaceNameSnake }}.go"
      inpackage: true
      recursive: true
      with-expecter: true
